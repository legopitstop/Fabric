package com.legopitstop.minecoin.item;

import net.minecraft.item.DyeableItem;

public class DyeableCardItem extends CardItem implements DyeableItem {
    public DyeableCardItem(Settings settings) {
        super(settings);
    }
}
